name: Build & Deploy Portfolio

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # 3. Install dependencies
      - name: Install dependencies
        run: npm install

      # 4. Generate README
      - name: Generate README
        run: node scripts/generate-readme.js

      # 5. Generate HTML
      - name: Generate HTML
        run: node scripts/generate-html.js

      # 6. Commit changes back to MAIN
      - name: Commit & Push changes to main
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update generated readme and index"
          file_pattern: "README.md index.html"

      # 7. Prepare dist folder
      - name: Prepare dist
        run: |
          mkdir dist
          cp index.html dist/
          cp README.md dist/
          cp -r css dist/
          cp -r assets dist/ || true
          mkdir -p dist/scripts
          cp scripts/main.js dist/scripts/

      # 8. Deploy
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.PROFILE_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          commit_message: "chore: deploy portfolio"
